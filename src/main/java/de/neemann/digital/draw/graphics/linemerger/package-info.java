/**
 * Classes used to merge the circuits lines to a long path.
 * Used to create a better locking SVG result because paths are as long as possible.
 * Only visible if the wires are not horizontal or vertical.
 *
 * @author hneemann
 */
package de.neemann.digital.draw.graphics.linemerger;
